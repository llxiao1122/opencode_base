# Cipher 系统全面审计报告 V8.0

审计 11 文件, ~3200 行。总问题 46 (7 HIGH / 18 MEDIUM / 21 LOW)

---

## 🔴 HIGH (7)

| # | 文件 | 行 | 问题 |
|---|------|----|------|
| H1 | 全局 | 多处 | **缓存一致性问题** — `_apply_changes_direct`、`sync_entity_index`、`builder.build()` 都直接写 `entity_index.json`，不通知 `shared/entity.py` 的 `_entities_cache`。后续 `load_entities()` 返回脏数据。 |
| H2 | `task_query.py` | 88-100 | **`_filter_tasks` 接受 `date_range` 参数但完全忽略** — 无论查 today/tomorrow/week，都返回所有未完成任务。范围筛选彻底失效。 |
| H3 | `registry.py` | 32-42 | **handler 传参语义错位** — Fast Path 传原始消息全文，Agent Path 通过 `param_map` 传参数化值（如 `"today"`、`"灭火器周期"`）。同一 handler 在不同路径下收到不同含义参数。 |
| H4 | `entry.py` | 69-98 | **`_apply_changes_direct` 脏写** — 直接写 entity_index.json 但不置 `_entities_cache = None` |
| H5 | `change_detector.py` | 257-298 | **`sync_entity_index` 脏写** — 写回文件后不清除全局缓存 |
| H6 | `builder.py` | 130-191 | **`build()` 脏写** — 写回后不通知缓存层 |
| H7 | `tasks.json` | 多处 | **Action/Subtask 文本严重重复** — 如 `"完成完成7月份盘点报告并推送OA完成7月份盘点报告并推送OA"` |

## 🟡 MEDIUM (18)

| # | 文件 | 行 | 问题 |
|---|------|----|------|
| M1 | `entry.py` | 36-37 | `_CHANGE_KW` 与 `change_detector.py` 关键字列表重复，改一处忘另一处则静默失效 |
| M2 | `entry.py` | 39-53 | `_index_built` 在 build() 失败后仍置 True，永不重试 |
| M3 | `entry.py` | 94 | `data["_meta"]["updated"]` 无 key 保护 |
| M4 | `registry.py` | 161-170 | `validate_params` 不校验 enum 约束，LLM 可传非法值 |
| M5 | `registry.py` | 196-202 | `_apply_param_map` 不传播 schema 中的 default 值 |
| M6 | `registry.py` | 57-62 | `notification_push` 的 `param_map` 未映射 `target` 参数 |
| M7 | `task_query.py` | 多处 | 函数体内部 import re，`_parse_table_section` 导入未使用 |
| M8 | `task_query.py` | 11+ | Handler 签名未标注类型，`_get_daily_work(user_input)` 中 user_input 未使用 |
| M9 | `task_query.py` | 132-136 | `OrganizationModel` 导入位于双重循环内部 |
| M10 | `change_detector.py` | 36, 290 | `.get("pending_entities", [])` 在 JSON `null` 时返回 None |
| M11 | `entity_index.json` | 423-448 | 残缺实体名：`"资产+废旧)"`、`"危废)"` — 切割错误产生的残渣 |
| M12 | `entity_index.json` | 396-422 | 重复实体：`"张梦圆"` 与 `"张梦圆(休假中)"` — 状态拼入 name |
| M13 | `tasks.json` | 30+ | deadline 值不一致：部分 ISO 日期 `2026-07-28`，部分中文 `"明天"` |
| M14 | `tasks.json` | 16 | title 含解析残留 `"今00:00"` |
| M15 | 全局 | entry.py:117-123 vs registry.py:227-228 | Fast Path 与 Agent Path 调用同一 handler 但传参语义不同 |
| M16 | `engine.py` | 128-129 | `_fallback_search` 创建新空 RequestContext，丢失原 ctx 信息 |

## 🟢 LOW (21)

| # | 文件 | 行 | 问题 |
|---|------|----|------|
| L1 | `entry.py` | 138 | `h['c']` 直接访问无 `.get()` |
| L2 | `entry.py` | 226 | `result.replace("李林骁", "主人")` 可能污染文档中的人名引用 |
| L3 | `entry.py` | 19-21 | 重复的 sys.path 注入 |
| L4 | `entry.py` | 60 | import 路径风格不统一 |
| L5 | `engine.py` | 10, 21 | `Path` 死引用，`ROOT` 未使用 |
| L6 | `engine.py` | 240-242 | `_nullctx()` 每次调用都重新 import |
| L7 | `engine.py` | 92-93 | `_parse_decisions` 的 `else: break` 可能导致多决策截断 |
| L8 | `registry.py` | 8 | `Path` 死引用 |
| L9 | `registry.py` | 173-181 | `_clean_params` 提取 dict 参数值可能导致静默丢失 |
| L10 | `task_query.py` | 103-109 | `_format_fallback` 死代码 |
| L11 | `task_query.py` | 149-154 | `_name_matches` 死代码 |
| L12 | `task_query.py` | 71-76 | `_detect_scope` 硬编码 vs schema enum 不同步 |
| L13 | `task_query.py` | 57 | `deadline[:10]` 对短字符串脆弱，任务永不标记逾期 |
| L14 | `change_detector.py` | 146-147 | E3 置信度检查死分支 |
| L15 | `change_detector.py` | 244, 246 | E4 防卫性检查恒为 True |
| L16 | `change_detector.py` | 135 | E5 `entity["name"]` 无 KeyError 保护 |
| L17 | `schema.py` | 11 | `from typing import List` 未使用 |
| L18 | `schema.py` | 74-93 | RequestContext 含 7 个未用字段 |
| L19 | `entity.py` | 93 | `has_known_entity` 中 `name[0]` 无保护 |
| L20 | `entity_index.json` | 1140-1146 | 日期被作为实体索引 |
| L21 | 多处 | — | 错误处理风格不统一，部分缺 `exc_info=True` |

---

## 优先级建议

### P0（立刻修）
1. **缓存一致性** — 所有写 `entity_index.json` 的路径统一走 `save_entities()`
2. **`_filter_tasks` 日期范围** — 实现按 `date_range` 过滤或删除死参数
3. **handler 传参语义** — 统一 Fast Path 与 Agent Path 的传参方式

### P1（本周修）
4. **tasks.json 数据修复** — 修正文本重复、deadline 不一致
5. **entity_index.json 数据修复** — 删除残渣实体、合并重复实体
6. **`_CHANGE_KW` 去重** — 消除 entry.py 与 change_detector.py 的关键字重复

### P2（后续）
7. 死代码/死 import 清理
8. schema 未用字段清理
9. import 路径统一
10. 错误处理 `exc_info` 补全
